import 'package:flutter/material.dart';

class Artist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Artist'),
      ),
      body: Text('Halaman artist wehhhh ea'),
    );
  }
}
